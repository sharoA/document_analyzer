{# 🌐 依赖分析节点默认提示词模板 #}

{% macro dependency_analysis_prompt(service_identification, design_doc) %}
你是微服务依赖分析专家。请分析服务间的依赖关系：

服务识别：
{{ service_identification }}

设计文档：
{{ design_doc }}

请输出JSON格式的依赖分析结果。

输出格式：
```json
{
    "dependencies": {
        "服务A": ["依赖服务1", "依赖服务2"],
        "服务B": ["依赖服务3"],
        "服务C": []
    },
    "dependency_graph": {
        "nodes": [
            {
                "service": "服务名称",
                "level": 0,
                "type": "核心服务|支撑服务|基础服务"
            }
        ],
        "edges": [
            {
                "from": "源服务",
                "to": "目标服务",
                "type": "强依赖|弱依赖",
                "call_pattern": "同步|异步|事件"
            }
        ]
    }
}
```
{% endmacro %} 