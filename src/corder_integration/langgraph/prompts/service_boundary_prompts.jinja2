{#- 服务边界识别提示词模板 -#}

{% macro service_boundary_prompt(design_summary, architecture_summary, context_window=None) %}
你是微服务架构专家。基于设计和架构概要进行服务拆分。

设计概要：
{{ design_summary }}

架构概要：
{{ architecture_summary }}

{% if context_window %}
上下文信息：
{{ context_window }}
{% endif %}

请根据实际需求进行服务拆分，输出JSON格式结果：
{
  "identified_services": [
        {
            "name": "服务名称",
            "description": "服务描述",
      "responsibilities": ["职责1", "职责2"],
      "data_entities": ["实体1", "实体2"],
      "apis": ["接口1", "接口2"]
        }
    ],
  "summary": "服务拆分概要(不超过200字)"
        }

注意：请根据实际业务复杂度确定合适的服务数量，避免过度拆分。通常2-4个服务比较合适。
{% endmacro %} 